<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="72dp"
    android:gravity="center_vertical"
    android:orientation="horizontal"
    android:paddingHorizontal="16dp"
    android:background="@color/white">

    <!-- Rank Number -->
    <TextView
        android:id="@+id/txtRank"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:text="4"
        android:textColor="@color/text_secondary"
        android:textSize="16sp"
        android:fontFamily="sans-serif-medium" />

    <!-- Avatar -->
    <com.google.android.material.imageview.ShapeableImageView
        android:id="@+id/imgAvatar"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_marginStart="8dp"
        android:scaleType="centerCrop"
        android:src="@drawable/bg_avatar_circle"
        app:shapeAppearanceOverlay="@style/CircleImageView"
        app:strokeColor="@color/border"
        app:strokeWidth="1dp" />

    <!-- Name -->
    <TextView
        android:id="@+id/txtName"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_weight="1"
        android:text="User Name"
        android:textColor="@color/text_primary"
        android:textSize="15sp"
        android:fontFamily="sans-serif-medium"
        android:ellipsize="end"
        android:maxLines="1" />

    <!-- Points Badge -->
    <TextView
        android:id="@+id/txtPoints"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@drawable/bg_badge_points"
        android:paddingHorizontal="10dp"
        android:paddingVertical="4dp"
        android:text="500 FP"
        android:textColor="@color/primary_dark"
        android:textSize="12sp"
        android:fontFamily="sans-serif-bold" />

    <!-- Bottom Border (Separation) -->
    <!-- This is usually handled by the RecyclerView decorator or by putting a View at the bottom of the layout -->
</LinearLayout>
